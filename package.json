{
  "name": "utility-agent-generator",
  "version": "0.0.1",
  "description": "Generate datasets using a utility agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "test": "jest --coverage",
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "pretest": "npm run lint"
  },
  "author": "Brad Sneade",
  "license": "MIT",
  "dependencies": {
    "utility-agent": "^0.9.10"
  },
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
